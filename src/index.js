import React from 'react';
import ReactDOM from 'react-dom';
import './main.css'; // Import the CSS file for full-page styling
import ThreeScene from './ThreeScene';

ReactDOM.render(
  <React.StrictMode>
    <ThreeScene />
  </React.StrictMode>,
  document.getElementById('root')
);
